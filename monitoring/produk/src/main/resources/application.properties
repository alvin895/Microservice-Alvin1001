spring.application.name=produk

server.port=8081

# H2 DATABASE
spring.datasource.url=jdbc:h2:mem:produk;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=123456

# H2 CONSOLE
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# EUREKA
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://eureka:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=produk
eureka.instance.instance-id=${spring.application.name}:${server.port}
spring.cloud.compatibility-verifier.enabled=false

# JWT
jwt.secret=abcdefghijklmnopqrstuvwxyz123456
jwt.expiration=86400000

# SECURITY LOG
logging.level.org.springframework.security=DEBUG

# MATIKAN CEK KOMPATIBILITAS SPRING CLOUD
spring.cloud.compatibility-verifier.enabled=false

# =========================
# ACTUATOR
# =========================

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.prometheus.enabled=true

# =========================
# PROMETHEUS
# =========================

management.prometheus.metrics.export.enabled=true
management.metrics.export.prometheus.enabled=true